# Repository Audit — Reference Prompt

Вставить этот файл целиком в начало аудита любому AI.  
Перед запуском — убедиться что блок «Контекст проекта» актуален.

---

## Перед началом — синхронизация

Аудит на устаревшем snapshot бесполезен. До чтения кода:
1. Прочитать последние 10 коммитов: `git log --oneline -10 main`
2. Зафиксировать HEAD: `git rev-parse main` — указать SHA в начале отчёта
3. Пробежать по `tasks/lessons.md` и `git log --grep=fix` за последний месяц — не повторять уже починённое

Если найден баг — убедиться что он **есть в текущем HEAD**, а не в кеше.

---

## Контекст проекта

```
Тип проекта: чат-бот для маркетплейсов (Wildberries, Ozon) с веб-интерфейсом
Стек: Python / FastAPI (web/app.py) + статический HTML (wb-reviewbot-v3.html)
Главный язык: Python
Коннекторы: connectors/wb_connector.py, connectors/ozon_connector.py
AI-провайдеры: openai, anthropic, gemini, deepseek — все в web/app.py:api_generate()
Внешние API: Wildberries API, Ozon API (через коннекторы), AI-провайдеры

CI/CD: automerge.yml (claude/** и cursor/** → main напрямую)
После push: syntax check + check_consistency.py → merge в main → удаление ветки

SSOT этого проекта:
  AI-провайдеры    → web/app.py:api_generate() (список: openai, anthropic, gemini, deepseek)
  git workflow     → .github/workflows/automerge.yml (claude/**, cursor/** → main)

Вторичные источники (должны совпадать с SSOT):
  CLAUDE.md §AI-провайдеры    → должен совпадать с web/app.py:api_generate()
  CLAUDE.md §Рабочий процесс  → должен совпадать с automerge.yml (без dev)
  scripts/check_consistency.py → должен проверять все эти соответствия
```

**НЕ проверять** (нерелевантно для этого проекта):
- multi-user изоляция и RBAC
- rate limiting и abuse prevention (нет публичного деплоя)
- GDPR / compliance
- Docker / Kubernetes / horizontal scaling
- §9 НАБЛЮДАЕМОСТЬ — нет runtime monitoring

---

## Pipeline — 3 pass

Один pass не справляется с объёмом. Запускать как отдельные сессии:

| Pass | Секции | Фокус |
|------|--------|-------|
| 1 — Корректность | §1, §3, §8 | SSOT sync, чистота слоёв, обработка ошибок |
| 2 — Безопасность + документация | §6, §7, §5 | security, dead code, docs vs reality |
| 3 — CI + архитектура | §4, §11, §12 | CI/CD, дизайн, freshness |

Каждый pass — свой мини-отчёт в формате §«Формат отчёта».

---

## Чеклист аудита

### 1. СИНХРОНИЗАЦИЯ (SSOT → вторичные источники)

SSOT: `web/app.py:api_generate()` (список провайдеров), `automerge.yml` (git workflow)

- [ ] Список провайдеров в `CLAUDE.md §AI-провайдеры` совпадает с `web/app.py:api_generate()`: `openai`, `anthropic`, `gemini`, `deepseek`
- [ ] `CLAUDE.md §Рабочий процесс` описывает `claude/... → main` напрямую (нет dev-стейджа)
- [ ] `automerge.yml` совпадает с описанием в `CLAUDE.md §Рабочий процесс`
- [ ] `CLAUDE.md` не упоминает `Flask` (стек — FastAPI)
- [ ] `scripts/check_consistency.py` проверяет все ключевые соответствия автоматически
- [ ] Добавление нового провайдера требует правки только в `web/app.py:api_generate()` и `CLAUDE.md §AI-провайдеры`

### 2. ВНЕШНИЕ API И КЛИЕНТЫ

- [ ] API-ключи WB/Ozon не захардкожены — только в `.env`
- [ ] `.env` в `.gitignore`, не попал в историю git
- [ ] API-ключи AI-провайдеров не захардкожены — только в `.env`
- [ ] Обработка ошибок при недоступности внешних API (WB, Ozon, AI-провайдеры)
- [ ] Тайм-аут на запросы к коннекторам

### 3. ЧИСТОТА СЛОЁВ

- [ ] `connectors/wb_connector.py` и `connectors/ozon_connector.py` содержат только логику API — не бизнес-логику
- [ ] `web/app.py:api_generate()` — единственное место выбора провайдера
- [ ] Статический HTML `wb-reviewbot-v3.html` не содержит API-ключей

### 4. CI/CD

- [ ] `automerge.yml` триггер ограничен `claude/**` и `cursor/**`
- [ ] `check_consistency.py` запускается в CI до merge
- [ ] При конфликте мержа — abort + exit 1, не зависает
- [ ] Нет `--no-verify`, нет force push в main
- [ ] Ветка удаляется автоматически после мержа (`--delete`)

### 5. ДОКУМЕНТАЦИЯ vs РЕАЛЬНОСТЬ

**Сканировать ВСЕ `.md` файлы:**
```
find . -name '*.md' -not -path './.git/*'
```

- [ ] `CLAUDE.md §AI-провайдеры` — список совпадает с `web/app.py:api_generate()`
- [ ] `CLAUDE.md §Инфраструктура` — пути файлов реально существуют
- [ ] `CLAUDE.md §Рабочий процесс` — не упоминает `dev` или `promote.yml`
- [ ] Все пути в `.md` реально существуют в репо

### 6. БЕЗОПАСНОСТЬ

- [ ] Нет секретов в файлах репо (API-ключи, токены)
- [ ] `.env` в `.gitignore`: `git log --all -- .env`
- [ ] Входные данные из HTTP-запросов валидируются до передачи во внешние API
- [ ] Нет command injection в shell-вызовах
- [ ] `debug=False` в FastAPI продакшен-режиме
- [ ] Секреты не выводятся через `print` или в логах
- [ ] Секреты не выводятся в `run:` шагах CI через `echo`

### 7. МЁРТВЫЙ КОД

- [ ] Нет неиспользуемых файлов в `connectors/`
- [ ] Нет устаревших workflow файлов в `.github/workflows/`
- [ ] Все провайдеры в `CLAUDE.md §AI-провайдеры` реально используются в `web/app.py`

### 8. ОБРАБОТКА ОШИБОК

- [ ] Коннекторы возвращают понятные ошибки при сбое API
- [ ] `api_generate()` обрабатывает недоступность провайдера — fallback или понятная ошибка пользователю
- [ ] `automerge.yml` abort при конфликте, не зависает

### 9. НАБЛЮДАЕМОСТЬ

_Не применимо — нет runtime monitoring._

### 10. ЗАВИСИМОСТИ

- [ ] Python-зависимости зафиксированы в `requirements.txt`
- [ ] Нет неиспользуемых зависимостей

### 11. АРХИТЕКТУРНЫЙ СМЫСЛ

- [ ] Что можно удалить без потери функциональности
- [ ] Добавление нового провайдера: сколько мест трогать? (`web/app.py:api_generate()`, `CLAUDE.md`) — это норма
- [ ] Добавление нового коннектора: сколько мест трогать?

### 12. AUDIT FRESHNESS

- [ ] Указать HEAD main SHA в начале отчёта
- [ ] Если пункт чеклиста «уже починено» — отметить, не выписывать как новую проблему

---

## Формат отчёта

**Калибровка severity до написания:**

```
SEVERITY:
  BLOCKER  = потеря данных / runtime не работает / дыра в безопасности
  HIGH     = silent degradation / неверный результат / неверный биллинг
  MEDIUM   = риск maintainability / drift который выстрелит через месяц
  LOW      = косметика / расхождение в docs / стиль

CONFIDENCE:
  HIGH    = нашёл в коде, строку указал, воспроизводимо
  MEDIUM  = паттерн виден, точная строка не проверена
  LOW     = подозрение — помечать явно, не выписывать как факт
```

Каждая проблема строго в формате:

```
[SEVERITY] [CONFIDENCE]
Файл: path/to/file:line
Проблема: что конкретно не так
Последствие: что сломается в реальном использовании
Фикс: конкретное исправление
```

Завершить отчёт тремя блоками:
1. **Блокеры** — что мешает работе прямо сейчас
2. **Что сделано хорошо** — не пропускать
3. **Следующие 3 приоритета** — конкретные задачи в порядке важности

**Не писать:**
- общие советы без привязки к файлу
- «рассмотреть использование паттерна X»
- enterprise-рекомендации

---

## Вывод

Отчёт одним markdown файлом.
